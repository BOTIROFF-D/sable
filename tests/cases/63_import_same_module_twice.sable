// Один модуль двумя формами: «counter выполнен» печатается один раз,
// а счётчик общий — значит, тело выполнилось ровно однажды
import "lib/counter.sable" as c
import "lib/counter.sable" as { считать as ещё }

print(c.считать(), ещё(), c.считать())
