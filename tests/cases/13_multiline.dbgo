let text = `первая строка
вторая строка
третья`
print(text)
print(text.split("\n").len())
let name = "dbgo"
print(`вставки работают: ${name.upper()}
и на второй строке ${1 + 2}`)
print(`кавычки "двойные" и 'одинарные' внутри — без экранирования`)
print(`экранированная \` обратная кавычка`)
print(len(`а
б`))
