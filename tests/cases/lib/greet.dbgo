// Вспомогательный модуль для теста 40 — сам по себе тестом не является
const ЯЗЫК = "dbgo"
fn привет(кто) { return "Привет, ${кто}! Это ${ЯЗЫК}." }
fn удвоить(n) { return n * 2 }
struct Метка { текст
  fn громко() { return self.текст.upper() + "!" }
}
