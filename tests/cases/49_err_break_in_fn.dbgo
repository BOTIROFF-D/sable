// Из функции нельзя прервать цикл, который её вызвал: граница вызова
// сигнал не пропускает. Раньше это молча работало, и статическая
// проверка спорила с рантаймом.
fn прервать() { break }

for i in 0..3 {
  print("виток", i)
  прервать()
}
