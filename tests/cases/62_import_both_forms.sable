// Обе формы в одном файле: одному модулю нужно пространство имён, другому — одно имя
import "lib/greet.sable" as g
import "lib/counter.sable" as { считать }

print(g.ЯЗЫК, g.удвоить(2))
print(считать(), считать())
