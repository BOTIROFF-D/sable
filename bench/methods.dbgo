// Обращения к методам встроенных типов: самая частая операция в реальном коде
let xs = []
let s = "строка для работы"
let m = { a: 1, b: 2 }
let n = 0
for i in 0..120000 {
  xs.push(i)
  n += s.len() + m.len() + xs.len()
  if xs.len() > 8 { xs.pop() }
}
print(n)
